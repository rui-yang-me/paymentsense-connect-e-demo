## 最佳实践
检查现有访问令牌
为了避免对持卡人重复收费，我们建议您将创建的访问令牌与会话或订单关联存储。当支付页面刷新时，检查之前访问令牌的状态，如果未使用则在请求新令牌之前将其撤销。如果原始访问令牌已被使用且支付成功，则相应更新订单；否则请求新的访问令牌。
我们发现过这样的问题：页面加载时发放了新的访问令牌，但交易过程中出现连接中断，导致持卡人已被扣款却未显示支付完成页面。当持卡人刷新页面时，系统创建了新的访问令牌，他们被再次扣款。

## 等待预执行
为了对交易拥有最大控制权，我们建议使用等待预执行支付流程。只有在调用恢复端点时才会执行交易。这可以减少因连接问题或JavaScript错误导致持卡人被扣款而订单未更新的情况。

## Webhooks
由于分布式系统的特性，网络调用总有失败的可能。我们建议通过webhook更新您的订单。由于我们使用指数退避策略，我们将尝试调用您的端点长达2小时，以容忍中断或网络连接问题。
存储重复支付的过期日期
如果您进行重复支付，我们建议除了交叉引用外，还要存储过期日期。这样，如果原始卡片已过期，您可以选择重新获取卡片详情。

## 唯一订单ID
每个订单应在 OrderId 字段中传递唯一且可识别的订单ID，用于在故障排查时在网站和Connect-E之间对账交易。在拒绝后重新尝试支付时创建的额外访问令牌应使用相同的 OrderId。

## 建议测试
集成完成后，我们建议测试以下场景：
3DS支付流程。例如使用卡号 4456530000001096
标准支付流程。例如使用卡号 5200000000000056
测试被拒绝的支付。集成是否请求新的访问令牌并重新显示/更新支付表单？例如使用测试卡 4456530000001013
如果使用 PREAUTH 交易，集成能否成功 COLLECT 该 PREAUTH
如果集成使用交叉引用执行重复支付，这对3DS卡和标准卡是否都有效？

是否允许用户查看正在使用的卡片？
卡片过期后是否隐藏该选项？